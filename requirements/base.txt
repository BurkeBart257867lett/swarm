# Shared Base Dependencies
# Used across all REDACTED AI Swarm components

# Configuration & Environment
python-dotenv>=1.0.0        # Load .env variables

# Data Structures & Validation
pydantic>=2.0               # Type-safe configs, .character.json parsing

# HTTP & Networking
requests>=2.31.0            # API calls (DexScreener, Birdeye, CoinGecko, Solscan)
aiohttp>=3.9.0              # Async HTTP for streaming responses

# Utilities
rich>=13.0                  # Beautiful terminal output, NERV-style UI
typer>=0.9.0                # Modern CLI building (summon_agent.py, upgrade_terminal.py)
pyyaml>=6.0                 # YAML config file support

# JSON serialization for .character.json and contract files
json5>=0.9.0                # JSON5 support for comments in config files

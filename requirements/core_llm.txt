# Core Ollama & Local LLM Integration
# Used in python/ollama_client.py, python/run_with_ollama.py

-r base.txt                 # Include all base dependencies

# Local LLM Runtime
ollama>=0.3.0               # Official Ollama Python client

# OpenAI-Compatible API (for Ollama fallback)
openai>=1.0.0               # Works with Ollama's OpenAI-compatible endpoint

# Async/await for streaming
asyncio>=3.4.3              # Usually bundled, explicit for clarity
